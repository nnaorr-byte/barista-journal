---
name: יומן בריסטה חכם
description: יומן שוטים חכם לאספרסו ביתי — תיעוד, ניתוח והמלצות במעטפת של זכוכית קרמה
colors:
  espresso-night: "#190e06"
  mocha-elevated: "#2a1b10"
  card-roast: "#251710"
  input-bean: "#1e1209"
  crema-ink: "#f6ead8"
  latte-muted: "#c2a582"
  crema-amber: "#e8a960"
  crema-strong: "#ffcf95"
  crema-light: "#ffe3bd"
  terracotta-fab: "#b45830"
  leaf-good: "#93c47f"
  honey-warn: "#e0b45e"
  clay-bad: "#dd8171"
  latte-bg: "#f2e8d9"
  latte-elevated: "#f7eede"
  latte-card: "#fdf8ee"
  latte-input: "#f5ecdb"
  latte-ink: "#33241a"
  latte-muted: "#6b5540"
  latte-amber: "#a5642a"
  latte-amber-strong: "#8a5222"
  latte-crema: "#7d5027"
  latte-good: "#41682f"
  latte-warn: "#7a5a1d"
  latte-bad: "#ad4a39"
  fab-terracotta-light: "#cf7240"
  fab-terracotta-dark: "#98431f"
  fab-ink: "#fff6ec"
  danger-ink: "#ffd9cf"
  latte-danger-ink: "#8a2f1e"
  banner-ink: "#6e3f10"
  seg-ink: "#1d130a"
  latte-seg-ink: "#fff4e4"
typography:
  display:
    fontFamily: "Heebo, Segoe UI, Arial, sans-serif"
    fontSize: "3.6rem"
    fontWeight: 800
    lineHeight: 1
  headline:
    fontFamily: "Heebo, Segoe UI, Arial, sans-serif"
    fontSize: "1.3rem"
    fontWeight: 800
    letterSpacing: "-0.2px"
  title:
    fontFamily: "Heebo, Segoe UI, Arial, sans-serif"
    fontSize: "1.05rem"
    fontWeight: 700
    letterSpacing: "-0.1px"
  body:
    fontFamily: "Heebo, Segoe UI, Arial, sans-serif"
    fontSize: "1rem"
    fontWeight: 400
    lineHeight: 1.55
  label:
    fontFamily: "Heebo, Segoe UI, Arial, sans-serif"
    fontSize: "0.82rem"
    fontWeight: 600
rounded:
  sm: "12px"
  md: "16px"
  fab: "19px"
  pill: "999px"
spacing:
  sm: "8px"
  md: "12px"
  lg: "16px"
  card: "16px"
components:
  button-primary:
    backgroundColor: "linear-gradient(160deg, rgba(255,190,110,0.30), rgba(255,160,80,0.10))"
    textColor: "#ffe6c2"
    rounded: "15px"
    padding: "12px 24px"
  button-secondary:
    backgroundColor: "rgba(255,227,189,0.05)"
    textColor: "{colors.crema-ink}"
    rounded: "15px"
    padding: "12px 24px"
  chip:
    backgroundColor: "{colors.mocha-elevated}"
    textColor: "{colors.crema-ink}"
    rounded: "{rounded.pill}"
    padding: "10px 16px"
  chip-selected:
    backgroundColor: "linear-gradient(160deg, rgba(255,190,110,0.38), rgba(255,150,70,0.2))"
    textColor: "#fff1dd"
    rounded: "{rounded.pill}"
    padding: "10px 16px"
  card:
    backgroundColor: "{colors.card-roast}"
    rounded: "{rounded.md}"
    padding: "{spacing.card}"
  input:
    backgroundColor: "{colors.input-bean}"
    textColor: "{colors.crema-ink}"
    rounded: "{rounded.sm}"
    padding: "12px"
  nav-fab:
    backgroundColor: "linear-gradient(165deg, #cf7240 0%, #b45830 55%, #98431f 100%)"
    textColor: "#fff6ec"
    rounded: "{rounded.fab}"
    size: "58px"
---

# Design System: יומן בריסטה חכם

## 1. Overview

**Creative North Star: "הקרמה הזכוכית"**

זכוכית ענבר חמה על רקע אספרסו כהה — כמו שכבת הקרמה שמתיישבת על שוט מושלם. כל האלמנטים ה"חיים" של הממשק (כפתורים ראשיים, ניווט, כותרת בגלילה) עשויים זכוכית קפה: גרדיאנט ענברי שקוף-למחצה, גבול מוזהב, טשטוש רקע, וזוהר חם במגע. הרקע הוא לילה של אספרסו (#190e06) שבו פולי קפה בודדים צונחים באיטיות — אווירה, לא רעש. המערכת מדויקת כמו כלי מדידה (מספרים טבולריים, יחסים, טווחי יעד) אבל לעולם לא קרה: החום מגיע מהפלטה, מהקול העברי האנושי, ומרגעי חגיגה שמורים להישגים אמיתיים.

המערכת דוחה במפורש את מה ש-PRODUCT.md אוסר: לא SaaS תאגידי גנרי, לא גיליון אקסל יבש, לא אפליקציית משחק צעקנית, ולא רשת חברתית. זו חוויה אישית, של אדם אחד ומכונת האספרסו שלו.

**Key Characteristics:**
- זכוכית ענבר כשפת המותג — שמורה לאלמנטים אינטראקטיביים ראשיים בלבד
- כהה כברירת מחדל ("לילה של אספרסו"), עם מצב בהיר "לאטה" מלא ושקול
- רכיבים מוחשיים ובטוחים: נוכחות פיזית, פידבק מיידי בלחיצה, זוהר חם במגע
- מספרים הם גיבורים: טבולריים, גדולים, מדויקים
- תנועה שמסבירה מצב (150–320ms, ease-out-quint; עד ~620ms לציור גרף), לעולם לא קישוט — פרק 6

## 2. Colors

פלטת אספרסו: לילה כהה של קפה קלוי שעליו ענבר קרמה זוהר — Restrained עם accent אחד שעובד קשה.

### Primary
- **ענבר קרמה** (#e8a960): צבע ההדגשה היחיד. גרפים, טבעות פוקוס, גבולות פעילים, מילוי פסי התקדמות. שיאו — **קרמה חזקה** (#ffcf95) לערכים מספריים וכותרות משנה, ו**קרמה בהירה** (#ffe3bd) לכותרות ראשיות.

### Secondary
- **טרקוטה** (גרדיאנט #cf7240→#98431f): שמורה בלעדית לכפתור "שוט חדש" המרכזי (ה-FAB). מופיעה במקום אחד ויחיד במסך.

### Neutral
- **לילה של אספרסו** (#190e06): רקע הגוף. תמיד עם גרדיאנט עדין של אור ענברי בפינות.
- **מוקה מורם** (#2a1b10): משטחים משניים — אריחי סטטיסטיקה, צ'יפים, בלוקים פנימיים.
- **קלייה** (#251710): רקע כרטיסים (בפועל דרך גרדיאנט card עדין).
- **דיו קרמה** (#f6ead8): טקסט ראשי. **לאטה עמום** (#c2a582): טקסט משני — תמיד ≥4.5:1 על כל הרקעים.

### Semantic
- **עלה** (#93c47f) הצלחה/בטווח · **דבש** (#e0b45e) אזהרה · **חרס** (#dd8171) בעיה. בשלושתם: טקסט/גבול, לא מילוי מסך.

### מצב בהיר — "לאטה" (הטוקנים latte-* בחזית)
המצב הבהיר אינו שקיפות של הכהה — כל תפקיד מקבל ערך משלו, כהה יותר ורווי יותר כדי לשמור קונטרסט ≥4.5:1 על קרם: רקע **לאטה** (#f2e8d9) עם גרדיאנט כתמתם עדין, משטחים #f7eede/#fdf8ee, דיו #33241a, עמום #6b5540, ענבר #a5642a (חזק #8a5222), קרמה #7d5027, וסמנטיים כהים (#41682f / #7a5a1d / #ad4a39). גרדיאנטי הזכוכית עוברים לענבר-אדמה אטום יותר (עד 0.97 אטימות בצ'יפ נבחר) — הזכוכית "מתמלאת" באור במקום לזהור בחושך.

### Named Rules
**חוק הזכוכית.** אפקט הזכוכית (גרדיאנט ענברי + blur + גבול מוזהב) מותר אך ורק על: כפתורים ראשיים, צ'יפים נבחרים, מרכז הטיימר, הניווט התחתון, והכותרת בגלילה. זכוכית על תוכן סטטי — אסורה.

**חוק הטרקוטה האחת.** הטרקוטה מופיעה רק על ה-FAB. אלמנט שני בטרקוטה שובר את ההיררכיה של הפעולה הראשית.

## 3. Typography

**Display/Body Font:** Heebo (עם Segoe UI, Arial כגיבוי) — מתארח מקומית ב-woff2 עם unicode-range נפרד לעברית וללטינית.

**Character:** משפחה אחת במשקלים 400–800. עברית ראשונה, RTL מלא; לטינית רק למונחי בריסטה בינלאומיים (Bottomless, Yield) ותמיד עם עברית לצידה.

### Hierarchy
- **Display** (800, 3.6rem / בתוך הטבעת 2.5rem, tabular-nums): ספרות הטיימר בלבד. `direction: ltr`.
- **Recipe** (800, 2.4rem, line-height 1, -1.5px, tabular-nums): שלושת מספרי המרשם במסך הבית (מנה / עצור ב־ / בכוס). שלושתם באותו גודל בכוונה — אין ביניהם היררכיה, הם רצף אחד. `direction: ltr` על השורה בלבד.
- **Headline** (800, 1.3rem, -0.2px): כותרת האפליקציה, בצבע קרמה בהירה.
- **Title** (700, 1.05rem, -0.1px): כותרות כרטיסים (h2) — תמיד אייקון SVG 18px + טקסט, עם קו hairline מתחת.
- **Body** (400, 1rem, 1.55): טקסט רץ.
- **Label** (600, 0.82rem): תוויות שדות וטקסט משני, בלאטה עמום. רצפת גודל: 0.72rem — קטן מזה אסור.

### Named Rules
**חוק המספר הטבולרי.** כל ערך מספרי (דירוג, גרמים, שניות, אחוזים) מקבל `font-variant-numeric: tabular-nums`. מספרים שקופצים ברוחב בזמן ספירה/טיימר — באג.

## 4. Elevation

היברידי, בסדר הזה: (1) שכבות טונאליות — משטח בהיר יותר = קרוב יותר (לילה → קלייה → מוקה); (2) קו "קרמה" — border-top בהיר עדין על כרטיסים, כמו אור שנשבר בקצה; (3) צל רך אחד; (4) זוהר ענברי — שמור למצבי hover/בחירה/הישג בלבד.

### Shadow Vocabulary
- **צל כרטיס** (`0 1px 2px rgba(0,0,0,0.4), 0 6px 18px rgba(0,0,0,0.28)`): כרטיסים וטוסטים. היחיד במנוחה.
- **זוהר חם** (`0 0 16px–30px rgba(255,178,96,0.35)`): hover על זכוכית, צ'יפ נבחר, כרטיס-גיבור, שיא אישי.
- **טבעת חיתוך FAB** (`0 0 0 6px var(--bg-elevated)` + צל + זוהר): ה-FAB בלבד.

### Named Rules
**חוק הזוהר שהרווחת.** זוהר ענברי מופיע רק כתגובה — למגע, לבחירה או להישג. זוהר במנוחה מותר רק על כרטיס-הגיבור של מסך הבית.

## 5. Components

תחושה: **מוחשיים ובטוחים** — לכל רכיב נוכחות פיזית, פידבק מיידי (scale בלחיצה), וזוהר חם במגע.

### Buttons
- **Shape:** מעוגל-נדיב (15px; קטנים — גלולה 999px)
- **Primary:** זכוכית ענברית — גרדיאנט שקוף-למחצה, טקסט #ffe6c2, גבול מוזהב, inset highlight עליון. Hover: הגרדיאנט מתחזק + זוהר חם. Active: scale(0.97).
- **Secondary:** שקוף כמעט לגמרי (5% קרמה), גבול חצי-שקוף.
- **Danger:** גרדיאנט אדמדם שקוף עם טקסט #ffd9cf. פעולות הרסניות עוברות דרך ConfirmButton דו-שלבי — לעולם לא confirm() נטיבי.
- **Focus:** `outline: 3px` ענברי חצי-שקוף, offset 2px — אחיד לכל הכפתורים.

### Chips
- **Style:** גלולות (999px) על מוקה מורם, min-height 44px.
- **State:** נבחר = מילוי זכוכית ענברית + זוהר + משקל 700 + `aria-pressed`. בחירת דירוג = `role="radiogroup"`.

### Cards / Containers
- **Corner Style:** 16px · **Background:** גרדיאנט קלייה עדין · **Border:** border-soft עם border-top בהיר יותר (קו הקרמה) · **Padding:** 16px · כרטיס `hero` (אחד למסך, לכל היותר): גבול ענברי + זוהר במנוחה + כותרת מוגדלת.
- **שקיפות הכרטיס:** הכרטיס שקוף-למחצה כדי שפולי הקפה הנופלים ייראו מבעדו. הצפיפות היא טוקן אחד — `--card-veil` (56% בכהה, 62% בבהיר) — ולא ארבעה ערכי rgba. **המשטחים שבתוך הכרטיס נשארים אטומים** (`--bg-elevated`: אריחי סטטיסטיקה, אריחי אימות, שכבת ההתראות): הפולים נראים במרווחים של הכרטיס, לא מאחורי הטקסט. זו ההיררכיה — זכוכית מסביב, משטח מלא מתחת למספרים.
- **`backdrop-filter` על כרטיס — אסור.** נוסה (blur(3px), 2026-07-28) והוסר (2026-07-30). הוא נראה טוב על כרטיס בודד, אבל הוא חל על *כל* כרטיס: במסך הנתונים הצטברו 2.51 שטחי-מסך של משטח שדוגם מחדש את מה שמאחוריו בכל פריים, מעל canvas קבוע שמצייר 21 פולים ב-60fps. iOS ויתר על ציור מחדש של הסרגל התחתון והכותרת הדביקה תוך כדי גלילה, והם נראו נסחפים ומתנתקים מהקצוות. **טשטוש-רקע שמור לשני משטחי ה-chrome בלבד** (כותרת בגלילה, ניווט תחתון) — שניים קבועים, לא עשרים משתנים. הסרתו הורידה את השטח המטושטש ל-0.08 שטחי-מסך.

### Inputs / Fields
- **Style:** רקע כהה שקוע (#1e1209), גבול רך, 12px radius. תווית מעל השדה (0.82rem, לאטה עמום).
- **Focus:** גבול ענברי + הילת box-shadow רכה (22% ענבר). בלי outline.

### Navigation
- זכוכית קפה קבועה בתחתית: רקע 88% מוקה + blur(14px). פריט פעיל: קרמה חזקה + `aria-current="page"`. במרכז — ה-FAB הטרקוטה, מורם 24px מעל הסרגל.
- **גריד של חמש עמודות שוות, לא `space-around`.** התוויות באורכים שונים ("בית" מול "נתונים"), ו-`space-around` מחלק את המרווח לפי רוחב הפריטים — מה שדחף את ה-FAB 7px לכיוון הצד הצר. `grid-template-columns: repeat(5, 1fr)` מציב את העמודה השלישית בדיוק על אמצע המסך ללא תלות בטקסט.
- **ה-safe-area *הוא* אזור מחוון הבית — לא מוסיפים לו ריפוד.** `padding-bottom: max(5px, env(safe-area-inset-bottom))`, לא `calc(7px + env(...))`. ה-`max()` שומר על מרווח סימטרי במכשירים בלי notch. הריפוד התחתון של `.app` נגזר מאותו inset — קבוע 100px חתך את סוף התוכן מתחת לסרגל במכשירים עם מחוון.

### שכבת ההתראות (`.alert-layer`)
פאנל אחד מעל ההמלצה, לא כרטיס: רקע ‎10% מהטון על מוקה מורם, גבול ‎45% מהטון, `--radius-sm`, בלי צל — אותה שפה של `.one-var-banner`. הטון מוזרק כ-`--alert-tone` מה-JSX (`--bad` לגיבוי דחוף, `--warn` לרוב, `--text-muted` למלאי נמוך), כך שאין כלל CSS לכל סוג התראה. מקופל: אייקון + שתי שורות + גלולת `+N` + chevron. פתוח: כותרת ‎0.72rem/800 עם letter-spacing, ואז שורה לכל התראה עם פעולה אחת ב-`.btn.small`, מופרדות ב-hairline. הפתיחה דרך `.collapse` (grid-rows), לא `height`.

### מרשם ושורת אימות (`.recipe-row` / `.verify-row`)
המרשם הוא שלושה מספרים בגודל זהה עם `→` ביניהם, ומעליהם `.fresh-bar` — מסילה של ‎7px שבה מלבן ירוק שקוף הוא חלון הטריות וסמן קרמה של ‎3px הוא היום. מתחת למפריד: שלושה אריחי אימות (שניות / יחס / טחינה) על מוקה מורם. **אריח הטחינה מודגש בגבול ענברי ובטקסט `--accent-strong` רק כשההמלצה מזיזה אותו** — הוא המשתנה היחיד שמשתנה, ולכן היחיד שמקבל הדגשה.

### רצועת הכיול (`.dial-in-strip`)
מופיעה רק בתוך תהליך DIAL IN, בראש כרטיס ההמלצה ובראש כרטיס ה-Coach — אותו רכיב בשני המקומות, כדי שהתהליך ייראה אותו דבר לפני השוט ואחריו. אותה שפה של `.alert-layer`: רקע ‎10% ענבר על מוקה מורם, גבול ‎45%, `--radius-sm`, בלי צל. המשטח **אטום** (`--bg-elevated`) כי יש עליו טקסט — פולי הרקע לא עוברים מתחת למילים. שורת כותרת: אייקון מטרה + שם השלב ב-`--accent-strong`, ומספר השוט נדחק לקצה ב-`margin-inline-start:auto`. מתחתיה `.dial-in-track` — ארבעה קטעים של ‎4px: שלבים שהסתיימו ב-‎50% ענבר, **השלב הנוכחי ב-`--accent-strong`** (הבהיר ביותר — שם העין נחה), והבאים ב-hairline. הקטעים הם `<li>` בתוך `<ol>` עם `aria-current="step"`, לא `<div>` — זו רשימה מסודרת של שלבים.

### פס הטריות — שני שעונים, רכיב אחד
`FreshnessBar` מקבל `clock` ומחליף שלושה דברים בלבד: הגרדיאנט, תוויות הקצה ונקודת האפס. האורך נשאר 60 יום בשניהם, כך שהעין לומדת סקאלה אחת. **קלייה טרייה** — יום 0 הוא הקלייה, גרדיאנט warn→good→warn→bad (Degassing בהתחלה, שיא ~14, דעיכה מ-45), תוויות `קלייה · שיא ~14י' · דד-ליין 60י'`. **קלייה ישנה** — יום 0 הוא פתיחת השקית, אין שלב גזים ולכן הגרדיאנט מתחיל ירוק ונשבר ב-50% (יום 30), תוויות `פתיחה · מיטבי עד 30י' · דד-ליין 60י'`. בקלייה ישנה מוצגת גם שורת `גיל קלייה: N ימים (מידע רקע)` — שני מספרים שונים שאסור להם להתחזות לאחד.

### סדר הפולים
הפולים של השוט האחרון ראשונים, לפי `lastUsedAt DESC` שנגזר מהשוטים ולא נשמר כשדה. פולים בלי שוטים נופלים לסוף לפי סדר ההוספה. הכרטיס הראשון הוא כמעט תמיד זה שרוצים לגעת בו.

### תג "במסגרת כיול" ביומן
שוט ששייך לסשן כיול **שעדיין רץ** נושא `.badge accent` ליד שם הפולים, ובפתיחה שורה עם אייקון מטרה שמציינת את השלב ומספר השוט בסשן. הסימון יורד ברגע שהסשן נסגר — לא נשמר כהיסטוריה. הסיבה: כל עוד הכיול פתוח השוט הוא צעד בתהליך ולא רשומה עצמאית, ומי שגולל ביומן צריך לדעת למה המספרים קופצים. אחרי הסגירה הוא שוט ככל שוט, והתג היה הופך לרעש.

### מסלול הכיול (`.dial-in-ladder`) — שורות שהוצאו מהחישוב
שוט שסומן "נחנק" או "לא מייצג" נשאר ברצף (הוא באמת קרה) אבל ב-`opacity: 0.55` עם `.badge` צמוד ו-`line-through` על עמודת "לאן" — ההמלצה שניתנה עליו לא מובילה לשום מקום. מחיקה מהרשימה הייתה משכתבת את ההיסטוריה; השארה בלי סימון הייתה נראית כמו מדידה שהכיול נשען עליה.

### מסלול הכיול (`.dial-in-ladder`)
רשימה מסודרת בתוך `.dial-in-strip` — שורה לשוט: מספר, מה הוזן, ולאן המוח שלח (`← טחינה 19`). כיול הוא רצף, ומסך שמראה פריים אחד ממנו מסתיר את ההיגיון. השורות מופרדות ב-hairline (לא כרטיסים — היררכיה מקוננת היא בדיוק מה שגורם לזה להיראות שגוי), `font-variant-numeric: tabular-nums` כדי שהעמודות לא ירקדו, ו"לאן" ב-`--accent-strong` עם `white-space: nowrap`. **השורה האחרונה בלבד מקבלת `--text` ומשקל 600** — היא ההווה, השאר הן הקשר. במסך הבית `limit={3}` והשורות שהושמטו מוכרזות במילים ("2 שוטים קודמים לפני אלה"), לא נחתכות בשקט.

### מדדי עקביות במקום מדדי פסגה
"השוט הטוב ביותר אי פעם" הוסר מהבית (כרטיס ואריח). מדד פסגה לא עונה על השאלה שנאור באמת שואל — כמה בקרים יצאו כמו שרצה. במקומו אריח **"פער שניות באותן הגדרות"**: הפיזור בזמן החליטה על טחינה ומנה זהות בדיוק, אחרי נטרול שיפוע ההזדקנות של השקית. מתחתיו שתי שורות פרוזה — אחת שמתרגמת את המספר ("זה הפיזור הטבעי שלך" מול "הפער הזה הוא ההכנה"), ואחת שאומרת אם הפולים בכלל מזיזים את הזמן. שתיהן מ-`services/aging.ts`, אותה מדידה שמזינה גם את התראת ההכנה במוח.

### השוואה לשוט הקודם (`.vs-prev`)
שורה שקטה מתחת ל-RatingPicker, מופרדת ב-hairline: תווית עמומה + שלושה צ'יפים (פחות טוב · זהה · טוב יותר). **אופציונלי, ולחיצה חוזרת מבטלת** — שדה חובה נוסף בטופס היה מאט את התיעוד. הסיבה: הדירוג המוחלט דחוס בקצה העליון (45 השוטים הראשונים כולם 7–10), ו-9 אחד לא נבדל מ-9 אחר.

### קיבוץ היומן לפי פולים (`.bean-group` / `.bean-group-btn`)
היומן מקובץ לפי **סוג פולים**, לא לפי שקית — שקית שנייה מאותם פולים היא המשך של אותו הקשר, רק תאריך הקלייה השתנה. הקבוצה הראשונה (הפולים שבשימוש עכשיו) פתוחה, השאר כפתורי מסגרת מקופלים: שם הפולים + מספר השוטים + chevron מסתובב. הכפתור הפתוח מקבל גבול ענבר ‎45% ורקע ‎6% — אותה שפה כמו `.chip.selected`, בלי הצללה (הקבוצה אינה כרטיס בתוך כרטיס). כל קבוצה מגבילה ל-10 שוטים עם "הצג עוד N" משלה. **חיפוש או סינון מבטלים את הקיבוץ** ומחזירים רשימה שטוחה על פני כל הפולים: הקיבוץ עונה על "מה קורה עכשיו", והחיפוש שואל שאלה אחרת.

### דגלי אמינות על רשומה (`.flag-toggle`)
שני מתגים שמצהירים ששורה ביומן אינה מדידה: "המכונה נחנקה" ו"לא מייצג את הפולים". לא צ'יפ (זו לא בחירה מתוך קבוצה) ולא שדה רגיל (זו לא תכונה של השוט אלא של אמינותו) — שורה עם תיבת סימון על משטח מורם, `--radius-sm`, hairline. במצב מסומן הגבול והרקע עוברים ל-`--warn` (‎55%/‎8%) דרך `:has(input:checked)` — האזהרה היא המצב, ולכן היא נושאת את הצבע. ביומן הדגלים מופיעים כ-`.badge` ליד שם הפולים: "נחנק" ב-warn, "לא בחישוב" נייטרלי — האחד הוא אירוע, השני החלטה.

### מסנן הפולים בעמוד הנתונים
דרגת טחינה, זמן חליטה ויחס תלויים בפולים; ערבוב שתי שקיות בגרף אחד מייצר "סחיפה" שהיא בסך הכול החלפת פולים. לכן בראש עמוד הנתונים יושב `<select>` "הפולים בניתוח", ברירת מחדל = הפולים שבשימוש עכשיו, ואופציה אחרונה "כל הפולים יחד". מוצג רק כשיש יותר מסוג פולים אחד בהיסטוריה.

**המסנן חל על כל הקטגוריות** — מבט על, איכות, מגמות, עקביות ועלות. עד אוגוסט 2026 ששת הכרטיסים ש"מודדים אותך ולא את השקית" התעלמו ממנו ונשאו תג `כל הזמנים`, וזו הייתה טעות: הבחירה במסנן נראתה כאילו היא עושה משהו ולא עשתה. `DashboardScreen` מקבל `beanId`, ציון העקביות נגזר מהשוטים המסוננים, `CostDashboard` מצמצם גם את השקיות (כך ש"הושקע בפולים" מדבר על אותה שקית), וסשני הכיול מסוננים לפי הפולים של השקית שכוילה. השוואת הפולים במבט על נעלמת כשמסננים — אין מה להשוות פולים לעצמם.

**היוצא מן הכלל היחיד: עקומת הטריות.** היא לא שואלת "כמה טובים הפולים האלה" אלא "בכמה ימים מהקלייה הפולים בשיא **אצלי**" — שאלה שנענית רק מהצטברות של שקיות רבות. סינון לשקית אחת היה משאיר נקודות בודדות ומקרי. היא נשארת גלובלית ונושאת את התג `כל הזמנים` (`AllTimeBadge`), שמופיע רק כשהמסנן פעיל — ומכיוון שהיא היחידה, התג הוא היום סימן להיוצא מן הכלל ולא כלל.

כשאין מספיק נתונים בפולים שנבחרו, הכרטיס אומר מה חסר ("צריך לפחות 4 שוטים… כרגע יש N") במקום להיעלם בשקט — כרטיס נעדר נקרא כתקלה.

גרף הטחינה במצב "כל הפולים" מקבל **קווי הפרדה אנכיים** (`LineChart markers`, מקווקו ‎3 4 ב-`--text-muted`, תווית שם הפולים) בכל החלפת פולים — הקפיצה שאחרי הקו היא כיול חדש, לא סחיפה.

### Signature: טבעת הטיימר
SVG עגול עם מסילה, קשת "חלון יעד" ירוקה שקופה, וקשת התקדמות שצבעה חי (ענבר → ירוק בחלון → אדום מעבר לו). בתוך החלון הטבעת זוהרת בפעימה; עצירה בתוכו — פעימת "בול" חד-פעמית. במרכז: כפתור זכוכית עם הספרות.

## 6. Motion

תנועה כאן מסבירה מצב: פידבק, מעבר, טעינה, חשיפה. אין כוריאוגרפיית טעינה של עמוד ואין חשיפות-בגלילה — נאור באמצע משימה במטבח, לא גולל דף שיווקי. עודף תנועה הוא הסיכון האמיתי, לא חוסר.

### Easing
- **`--spring`** (`cubic-bezier(0.22, 1, 0.36, 1)`) — ease-out-quint. ברירת המחדל לכל שינוי מצב: כניסות, לחיצות, פתיחה וסגירה. **אין בו overshoot** — למרות השם.
- **`--ease-out-quart`** (`cubic-bezier(0.25, 1, 0.5, 1)`) — רכה מעט יותר, לתנועות ארוכות מ-400ms: ציור הגרפים והבזק אריח הטחינה.
- easing קפיצי או אלסטי — אסור. גם `--speed` (0.22s) נשאר ברירת המחדל למעברי צבע וגבול.

### Named Rules

**חוק מצב המנוחה.** כל `@keyframes` של חשיפה מגדיר **רק `from`**, ומצב המנוחה של האלמנט ב-CSS הוא המצב הסופי. גרף שהאנימציה שלו לא רצה — `prefers-reduced-motion`, טאב מוסתר שמקפיא את המנוע, דפדפן ישן — עדיין מוצג במלואו. הניסוח ההפוך (בסיס שקוף + `forwards`) שולח תוכן ריק לאוויר ברגע שהאנימציה לא מתרחשת.

**חוק הסטאגר היחיד.** לכל תוכן מגיעה כוריאוגרפיה אחת. כשההורה נכנס מדורג, הילדים נכנסים איתו — `.cat-swap .stat-tile` ו-`.scope-swap .stat-tile` מקבלים `animation: none`, אחרת אותם מספרים עוברים שתי כניסות שונות בו-זמנית.

**תקרת סטאגר.** ההשהיה נגזרת מ-`--i` שנשתל ב-JSX (`style={{ '--i': i }}`), והתקרה יושבת ב-CSS: `calc(min(var(--i, 0), 14) * 26ms)`. הסטאגר לא גדל עם הנתונים — 60 נקודות פיזור לא מייצרות 1.5 שניות של המתנה.

### Chart Draw-On (`charts.tsx`)
הכרטיס נכנס בתנועה, ולכן גם הנתונים שבתוכו חייבים להיכנס — אחרת הכרטיס זז והמידע כבר גמור.

| מחלקה | מה קורה | איך |
|---|---|---|
| `.chart-line` | הקו נמשך מההתחלה לסוף, 0.62s | `pathLength="1"` מנרמל את אורך הקו ל-1, ו-`stroke-dashoffset` יורד מ-1 ל-0. אין מדידה ב-JS |
| `.chart-line-overlay` | הממוצע הנע דוהה פנימה אחרי הקו הראשי | הוא כבר מקווקו (`6 5`), ומשיכת dash-offset הייתה מתנגשת בו |
| `.chart-bar-x` | בר אופקי גדל מימין לשמאל (RTL), 0.46s | `scaleX` עם `transform-box: fill-box` ו-`transform-origin: right center` — **לא `width`** |
| `.chart-bar-y` | בר אנכי גדל מהבסיס, 0.42s | `scaleY` עם `transform-origin: bottom center` — **לא `height`** |
| `.chart-dot` | נקודה עולה מ-scale 0.4, 0.24s, בסטאגר | `transform-box: fill-box` הכרחי, אחרת הנקודה נפתחת מפינת ה-SVG ולא ממרכזה |
| `.chart-ink` | תווית ערך דוהה **אחרי** הצורה שהיא מתארת | השהיית בסיס 0.2s ועוד סטאגר |
| `.chart-axis` | צירים, תוויות ורשת דוהים ב-0.18s | הרקע הראשון, הנתונים אחריו |

### מעבר בין תצוגות במסך הנתונים
שתי החלפות, שתי תנועות — כי הן אומרות דברים שונים:
- **`.cat-swap`** — החלפת קטגוריה מחליפה **אילו כרטיסים** מוצגים. כניסה מדורגת (`card-in`, 0.28s), מוגבלת ל-4 השהיות (45/90/135/180ms) ואז שטוחה.
- **`.scope-swap`** — החלפת הפולים משאירה את אותם כרטיסים ומחליפה את **משמעות המספרים**. הצלבה אחת קצרה (`scope-fade`, 0.18s מ-`opacity: 0.25`), בלי להזיז תוכן שנקרא באותו רגע.

איזו מהן רצה נקבע ב-`useState` במקום שבו ההחלפה קורית, לא ב-`ref` בזמן רינדור — StrictMode מרנדר פעמיים ומזהה שגוי היה מתחלף בין הפעלות.

### שלד טעינה (`.skeleton`)
`useLiveQuery` מחזיר `undefined` בפריים הראשון. במקום מסך ריק שקופץ לתוכן — שלד ששומר את הצורה של מה שבא (`ScreenSkeleton` / `SkeletonCard`), עם `aria-busy="true"` כדי שקורא מסך לא יקריא אותו.

הסריקה היא **`background-position` על גרדיאנט, לא `opacity`** — עמעום קבוע על אלמנטים חוזרים בתוך תוכן נגלל הוא בדיוק העלות שמסיטה את ה-chrome ב-iOS (ראה ה-Don't על `opacity`).

### מספרים (`CountUp`)
- **בכניסה למסך** — מאפס, 600ms, ease-out-quart מחושב ב-JS (`1 - (1-t)⁴`).
- **בשינוי ערך חי** — **מהמספר שמוצג באותו רגע**, 260ms. אנימציה שנקטעת באמצע ממשיכה מהערך הנוכחי ולא קופצת.

הגרסה הראשונה ספרה תמיד מאפס, וכל עדכון חי — שוט חדש שהזיז ממוצע מ-8.4 ל-8.6 — הפיל את המספר ל-0.0 והטיס אותו חזרה. זה נקרא "הנתונים נטענים מחדש", לא "הערך השתנה". בהחלפת מסנן או קטגוריה הספירה **כן** יוצאת מאפס: שם הפאנל נבנה מחדש, וזו כניסה ולא עדכון.

### רגעי delight
שמורים למה שהרוויח אותם: `Celebration` (קנבס) בדירוג 9+ ובשיא אישי לפולים, `zone-hit` בעצירה בתוך חלון הזמן, `tile-settle` (0.42s) על אריח הטחינה כשההמלצה מזיזה אותו — עם `key` לפי ערך הטחינה, אחרת האנימציה רצה פעם אחת בטעינה הראשונה בלבד. סמן "היום" בפס הטריות מחליק 0.32s למקומו, כך שהפס נקרא כציר זמן ולא כגרפיקה.

## 7. Do's and Don'ts

### Do:
- **Do** השתמש בטוקנים בלבד — גם ב-inline styles (`var(--good)`, לא hex).
- **Do** שמור על קונטרסט ≥4.5:1 בשני המצבים; מצב בהיר מקבל ערכי צבע משלו, לא שקיפות של הכהה.
- **Do** לווה כל שינוי מצב בתנועת ease-out-quint של 150–320ms, וכבה הכל תחת `prefers-reduced-motion`.
- **Do** תן לכל רכיב אינטראקטיבי ≥44×44px ו-scale קטן בלחיצה (0.9–0.97).
- **Do** אייקוני קו SVG (currentColor, stroke ~1.7) בכל ה-chrome; אימוג'י מותר רק בקול ה-AI.
- **Do** רשת של שלושה אריחים מקבלת `.stat-grid.cols-3` — `auto-fit` שובר שלושה ל-2+1 ברוחב כרטיס ב-375px.

### Don't:
- **Don't** "SaaS תאגידי גנרי" — אסור דשבורד קר וחסר אופי; כל מסך נושא את חום הקפה.
- **Don't** "גיליון אקסל יבש" — אסור טבלת נתונים בלי פרשנות; כל מספר מקבל הקשר או המלצה.
- **Don't** "אפליקציית משחק צעקנית" — אסור קונפטי על כל פעולה; חגיגה רק בדירוג 9+, שיא או אבן דרך.
- **Don't** "רשת חברתית" — אסור פידים, השוואות והתראות מציקות.
- **Don't** easing קפיצי/אלסטי, או אנימציית width/height (רק transform/grid-rows). בר גדל ב-`scaleX`/`scaleY` עם `transform-box: fill-box`, לא ברוחב ובגובה.
- **Don't** חסימת `prefers-reduced-motion` שמאפסת רק את משכי הזמן. היא חייבת לאפס גם **`animation-delay` ו-`transition-delay`** — אחרת אנימציה עם `backwards` מחזיקה את מצב ה-`from` (שקוף) לאורך כל ההשהיה, כלומר מי שביקש פחות תנועה רואה תוכן נעלם. באג שחי בקוד עד שהסטאגר של הגרפים חשף אותו.
- **Don't** `@keyframes` שמגדיר `to` על אלמנט שמצב המנוחה שלו שקוף. ראה "חוק מצב המנוחה" בפרק 6.
- **Don't** transform / filter / perspective / contain על `.app` או על כל עוטף אחר של ה-chrome — הם הופכים את העוטף לבלוק-מכיל, ו-`position: fixed` של הסרגל התחתון מפסיק להתייחס לחלון. הסרגל והכותרת עצמם **כן** נושאים `transform: translateZ(0)` בכוונה: שכבת קומפוזיציה משלהם, כדי ש-iOS לא יצרף אותם לשכבת הגלילה ויפסיק לצייר אותם תוך כדי תנועה.
- **Don't** `opacity` מתחת ל-1 על אלמנטים חוזרים בתוך תוכן נגלל. כל אחד מהם הוא שכבת קומפוזיציה נוספת מתחת ל-chrome הקבוע — בדיוק העלות שגורמת לסרגל להיסחף ב-iOS. לעמעום השתמש בצבע.
- **Don't** זכוכית דקורטיבית. שני היוצאים מן הכלל הם מכוונים ומתועדים: ה-chrome (topbar, ניווט תחתון) וכרטיסי התוכן, ששקיפותם קיימת כדי לחשוף את רקע הפולים הנופלים. משטח שיושב מתחת לטקסט או למספר נשאר אטום.
- **Don't** אימוג'י מערכת ב-chrome, פס צבע צדדי (border-left) על כרטיסים, או gradient-text.
